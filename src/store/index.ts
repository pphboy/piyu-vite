import { createStore } from "vuex";

export default createStore({
  state: {
    	login:false,
        userInfo:{
            headImage:null,
            username:null,
        }
    },
  mutations: {},
  actions: {},
  modules: {},
});
